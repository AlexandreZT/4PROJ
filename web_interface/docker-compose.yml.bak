version: "3.8"
services:
  web:
    image: backend
    container_name: "BCES"
    env_file: ./backend.env    
    build:
      context: ./backend/
      dockerfile: Dockerfile

    ports:
      - "5000:5000"

    volumes:
      - ./backend/:/usr/src/app